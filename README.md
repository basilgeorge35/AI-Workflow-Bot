This ia a Python automation project that classifies incoming support tickets using an AI model and routes them to category-specific folders/files.

Features:
- Uses OpenAI API to classify tickets into categories
- Demonstrates simple automation logic (classification + routing)
- Built with lightweight, readable code suited for a junior dev portfolio

Requirements:
1) openai>=1.0.0
2) pandas